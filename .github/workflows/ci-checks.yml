name: CI Checks

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  pipeline:
    name: CI Pipeline
    uses: PetroSa2/petrosa_k8s/.github/workflows/reusable/ci-pipeline.yml@main
    with:
      service-name: 'socket-client'
      image-name: 'yurisa2/petrosa-socket-client'
      python-version: '3.11'
      skip-docker-build: true
    secrets: inherit
